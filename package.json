{
  "name": "react-nesting-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "postinstall": "run-s install:*",
    "install:nested": "cd src/nested && npm i",
    "install:container": "cd src/container && npm i",
    "start": "serve -s build",
    "dev": "react-scripts start",
    "build": "react-scripts build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cpx": "1.5.0",
    "npm-run-all": "4.1.5"
  }
}
